import Home from "../home/home.componet";
import { AuthContainer } from "./auth.styles";

const Auth = () => {
  return (
    <AuthContainer>
      <Home />
    </AuthContainer>
  );
};

export default Auth;
